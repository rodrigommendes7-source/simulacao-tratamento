# Base de dados PostgreSQL (Neon, via integração da Vercel).
# Copiar para .env.local em desenvolvimento; na Vercel, definir nas variáveis
# de ambiente do projeto. Nunca commitar valores reais — .env*.local está no
# .gitignore.
DATABASE_URL="postgresql://utilizador:palavra-passe@host.neon.tech/base?sslmode=require"

